# Custom partition scheme for the Arduino Nano ESP32.
#
# Why: the Nano ESP32 ships with a FAT-based default partition
# (app3M_fat9M_fact512k_16MB) that has no SPIFFS/LittleFS region, so
# the arduino-littlefs-upload plugin fails with
# "Partition entry not found in csv file!"
#
# This file overrides the board default. Two 3 MB app slots (OTA-capable)
# and one ~9.94 MB LittleFS data partition. Total 16 MB.
#
# Name,    Type, SubType, Offset,   Size,      Flags
nvs,       data, nvs,     0x9000,   0x5000,
otadata,   data, ota,     0xE000,   0x2000,
app0,      app,  ota_0,   0x10000,  0x300000,
app1,      app,  ota_1,   0x310000, 0x300000,
spiffs,    data, spiffs,  0x610000, 0x9F0000,
